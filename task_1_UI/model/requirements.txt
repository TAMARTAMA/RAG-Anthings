# Core API server
fastapi
uvicorn[standard]

# nanoGPT typical deps
tiktoken
numpy
tqdm

# If you need any of these, uncomment:
# pandas
# pyyaml
# rich
# einops

# ⚠️ PyTorch is installed separately (platform-specific).
# See install.sh for the exact command (CPU or CUDA).
